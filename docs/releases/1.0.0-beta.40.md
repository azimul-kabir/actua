# Actua 1.0.0-beta.40

Beta 40 makes three synced report widgets real charts instead of placeholders
(Sankey flow diagram, per-category stacked bars, and drill-down from Cash
Flow/Calendar/Spending), fixes tag-highlighting and Manage Tags cosmetics,
relabels the bottom bar's quick-add tab, and fixes a Spending widget crash.

## Added

- Cash Flow, Calendar and Spending report widgets now support drill-down to their contributing transactions: tapping a Cash Flow period, a Calendar day or either Spending total opens the same transaction list sheet already used by Income vs Expenses and Custom Report (#498)
- The Sankey report widget is now a real flow diagram (an income node linked to per-category expense nodes, plus a "Remaining" node for unspent income) instead of two plain text columns, with tap-to-highlight interactivity matching the other synced report charts (#498)
- Saved custom reports using Actual's `StackedBarGraph` type now render as a real per-category stacked bar chart (one color per category, zero-filled so every bar keeps the same stack order) instead of a flat single-series bar chart, with tap-to-drill-down to a segment's contributing transactions (#498)

## Changed

- The bottom tab bar's quick-add tab is now labeled "Add" instead of "+ Add", since the bar's icon already carries a "+" glyph ([#501](https://github.com/azimul-kabir/actua/pull/501))

## Fixed

- Recognized `#tags` typed in a transaction's notes field now get the same pill background as saved tag chips, not just colored text (#505)
- Manage Tags' create/edit color picker now offers Actual web's full tag color set instead of a smaller, non-matching palette (#505)
- Manage Tags' per-row actions menu (View transactions/Edit/Delete) now opens anchored to the tapped row's icon instead of rendering at the top of the list (#505)
- Fixed a crash in the Spending widget's progress bar when one side (current vs. compare) fully dominated the other ([#500](https://github.com/azimul-kabir/actua/pull/500))

## Compatibility and safety

- Sankey, Stacked Bar and drill-down changes are rendering/interaction-only on top of the existing shared `ReportAggregator`/`CoreReportEngine` calculation layer; no report calculation changed and drill-down reuses the transaction list sheet already used by Income vs Expenses and Custom Report
- Tag pill and Manage Tags fixes are presentation-only and do not change stored tag data
- The tab bar label change and Spending widget crash fix are UI-only; no stored data or sync behavior is affected

## Since beta 39

This release includes #498 (Sankey flow diagram, per-category stacked bar
custom reports, and drill-down for Cash Flow/Calendar/Spending), #505 (tag
pill background and Manage Tags color picker/menu fixes), #501 (quick-add tab
label), and #500 (Spending widget progress bar crash fix).
