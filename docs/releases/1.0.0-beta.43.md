# Actua 1.0.0-beta.43

Beta 43 fixes two more report/summary calculation bugs (Crossover's expense
aggregation and the Accounts monthly summary's handling of off-budget
transfers), adds the remaining currencies from Actual's currency list, tunes
the app's out-of-the-box defaults for the Budget tab, Transactions & Accounts,
Display and the bottom Tab Bar, and fixes two back-gesture regressions.

## Added

- Added the remaining currencies from Actual's currency list to the currency setting ([#566](https://github.com/azimul-kabir/actua/pull/566))

## Changed

- Updated the out-of-the-box defaults for the Budget tab, Transactions & Accounts, Display, and the bottom Tab Bar ([#571](https://github.com/azimul-kabir/actua/pull/571))

## Fixed

- The back gesture from Customize Home and Customize Tab Bar now returns to the Settings page they were opened from instead of the Manage root ([#571](https://github.com/azimul-kabir/actua/pull/571))
- The back gesture's final stop before exiting the app now follows the Start page setting instead of always landing on Home ([#569](https://github.com/azimul-kabir/actua/pull/569))
- The Accounts tab's monthly summary now counts a transfer into or out of an off-budget account as an expense or income instead of excluding every transfer ([#562](https://github.com/azimul-kabir/actua/pull/562))
- The Crossover report widget's expense projection now matches transactions by category and date only, like the PWA, instead of also filtering by amount sign, transfers, and off-budget accounts - a refund posted to an expense category now nets against that month's spend instead of inflating the projected expense ([#560](https://github.com/azimul-kabir/actua/pull/560))

## Compatibility and safety

- The Crossover and Accounts monthly summary fixes are read-side aggregation/display changes only; no synced or stored transaction, budget or schedule data is read or written differently
- The new currency options are display-layer only; amounts remain stored as integer cents regardless of selected currency
- The updated defaults only change SharedPreferences fallback values for new installs; existing users' already-saved preferences are unaffected
- No database schema, sync protocol or migration changes

## Since beta 42

This release includes #571 (default settings update and Settings
sub-screen back-navigation fix), #569 (back-gesture exit tab follows the
Start page setting), #566 (remaining Actual currencies added to the
currency picker), #562 (Accounts monthly summary counts the on-budget leg
of an off-budget transfer), and #560 (Crossover widget's expense
aggregation matches upstream's unfiltered category query).
